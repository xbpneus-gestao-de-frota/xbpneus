# Dependências de Produção - Sistema XBPneus (ALINHADO)

# Core
Django==4.2.11
djangorestframework==3.14.0
python-decouple==3.8

# Database
psycopg2-binary==2.9.9  # PostgreSQL
dj-database-url==2.1.0

# Authentication & Security
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
django-axes==6.1.1
cryptography==42.0.5

# API Documentation
drf-spectacular==0.27.1
drf-spectacular-sidecar==2025.10.1  # Adicionado: Necessário para assets do Swagger UI

# Health Check
django-health-check==3.20.0  # Adicionado: Necessário para o health_check

# Cache & Performance (Versões alinhadas com requirements.txt)
django-redis==5.2.0
redis==4.5.5
hiredis==2.0.0

# Background Jobs
django-rq==2.10.2
rq==1.16.2

# File Storage
Pillow==10.2.0
# django-storages e boto3 não estão pinados no requirements.txt, mas estão instalados na versão 1.14.6 e 1.40.58, respectivamente.
# Mantendo o pin do requirements.txt para Pillow.

# Monitoring & Logging
sentry-sdk==1.43.0
python-json-logger==4.0.0  # Adicionado: Necessário para JsonFormatter

# Server
gunicorn==21.2.0
whitenoise==6.6.0

# Utilities
django-filter==24.1
python-dateutil==2.9.0
pytz==2024.1

# Excel/CSV
openpyxl==3.1.2
xlsxwriter==3.2.0

# Validations
validators==0.22.0

# Dependências que estavam no requirements.txt mas não no requirements-production.txt
# django-storages (1.14.6)
# boto3 (1.40.58)
# ffmpeg-python (0.2.0)
# django-allauth (65.1.0)
# pyfcm (2.1.0)
# Deixando de fora por enquanto, pois o foco foi nas libs *faltantes* e no alinhamento do cache.

